import React from 'react';
import txt from '../../text/reiki-session.txt';

const ReikiSession = () => {
	return (
		<div>
			<h3>Reiki Session</h3>
			<p>{txt}</p>
		</div>
	)
};

export default ReikiSession;
